---
import ArrowLink from "./ArrowLink.astro";
import type { Stat } from "../data/landing";

type Props = { stats: Stat[] };
const { stats } = Astro.props;

/**
 * Cells are padded on both sides so the copy never sits flush against a
 * divider. The cell that leads a row drops its left padding, so the first
 * figure stays optically aligned with the hero text above it — at sm the grid
 * is 2-up (odd children lead a row), at lg it is 4-up (only the first does).
 */
const CELL =
  "flex flex-col bg-white pt-5 pb-2 sm:px-6 sm:pt-6 sm:odd:pl-0 lg:pl-6 lg:first:pl-0";
---

{
  /*
  A <dl> group may contain only <dt> and <dd>, and the <dt> must come first —
  so the label leads in the DOM and `order` puts the figure above it visually.
*/
}
<dl
  class="grid grid-cols-1 gap-px border-t border-line bg-line sm:grid-cols-2 lg:grid-cols-4"
>
  {
    stats.map((stat) => (
      <div class={CELL}>
        <dt class="order-2 mt-1 text-sm leading-snug text-body">
          {stat.label}
        </dt>
        <dd class="order-1 font-mono text-2xl font-medium tracking-tight text-ink">
          {stat.value}
        </dd>
        <dd class="order-3 mt-3">
          <ArrowLink href={stat.href}>{stat.linkText}</ArrowLink>
        </dd>
      </div>
    ))
  }
</dl>
